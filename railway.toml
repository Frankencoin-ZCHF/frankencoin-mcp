[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/index.js --http"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
